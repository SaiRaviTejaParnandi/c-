string superReducedString(string s) {
    int len = s.size();
    for (int i=1;i<len;i++)
    {   
        if (s[i]==s[i-1])
        {
            s.erase(i-1,2);
            i=0;
        }
    }
    int x=s.size();
 if ( x==0)
 {
     
     return "Empty String";
 }
  else
  {
   return s;
  }
}
